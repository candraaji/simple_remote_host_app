import React from 'react';

const Button = () => <button type="button" class="btn btn-success" onClick={() => alert('this button from app3 components')}>App 3 Button</button>;

export default Button;
